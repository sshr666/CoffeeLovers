pages:
  stage: deploy
  script:
    - mkdir public
    - cp -r index.html styles.css script.js assets public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
